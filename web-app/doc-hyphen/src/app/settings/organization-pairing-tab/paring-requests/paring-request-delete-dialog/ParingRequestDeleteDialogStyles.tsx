import {makeStyles, tokens} from "@fluentui/react-components";

export const useParingRequestDeleteDialogStyles = makeStyles({
    dialogContent: {
        display: "flex",
        flexDirection: "column",
        gap: "26px"
    }
});
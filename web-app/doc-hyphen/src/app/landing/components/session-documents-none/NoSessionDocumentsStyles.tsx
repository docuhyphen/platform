import {makeStyles} from "@fluentui/react-components";

export const useNoSessionDocumentsStyles = makeStyles({
    noDocumentsContainer: {
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        height: '50%',
        gap: "16px"
    },
});
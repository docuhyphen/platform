import {makeStyles} from "@fluentui/react-components";

export const useSessionListPaginationStyles = makeStyles({

    container: {
        display: 'flex', alignItems: 'center', gap: '8px'
    },
});
import {makeStyles} from "@fluentui/react-components";

export const usePreLandingStyles = makeStyles({
    preLoadingContainer: {
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100%",
    },
});
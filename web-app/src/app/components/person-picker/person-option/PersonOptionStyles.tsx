import {makeStyles, tokens} from "@fluentui/react-components";

export const usePersonOptionStyles = makeStyles({
    persona: {
        width: "100%",
        paddingTop: tokens.spacingVerticalXS,
        paddingBottom: tokens.spacingVerticalXS,
    },
});

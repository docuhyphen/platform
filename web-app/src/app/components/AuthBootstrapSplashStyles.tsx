import {makeStyles} from "@fluentui/react-components";

export const useAuthBootstrapSplashStyles = makeStyles({
    splashContainer: {
        position: "fixed",
        inset: "0",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
    },
});

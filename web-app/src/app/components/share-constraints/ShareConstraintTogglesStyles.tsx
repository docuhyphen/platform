import {tokens, makeStyles} from "@fluentui/react-components";

export const useShareConstraintTogglesStyles = makeStyles({
    container: {
        display: "flex",
        flexDirection: "column",
        gap: tokens.spacingHorizontalSNudge,
    },
});

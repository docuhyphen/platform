import {makeStyles, tokens} from "@fluentui/react-components";

export const useViewModeToggleStyles = makeStyles({
    root: {
        display: "flex",
        gap: tokens.spacingHorizontalXXS,
    },
});

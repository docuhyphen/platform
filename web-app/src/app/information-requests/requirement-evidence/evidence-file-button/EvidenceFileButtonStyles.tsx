import {makeStyles} from "@fluentui/react-components";

export const useEvidenceFileButtonStyles = makeStyles({
    control: {
        display: "inline-flex",
    },
    hiddenInput: {
        display: "none",
    },
});

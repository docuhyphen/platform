import {makeStyles, tokens} from "@fluentui/react-components";

export const useAuditExportCategoriesFieldStyles = makeStyles({
    selected: {
        display: "flex",
        flexWrap: "wrap",
        gap: tokens.spacingHorizontalXS,
    },
});

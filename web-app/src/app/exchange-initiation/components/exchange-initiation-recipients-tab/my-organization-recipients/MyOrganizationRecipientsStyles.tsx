import {makeStyles} from "@fluentui/react-components";

export const useMyOrganizationRecipientsStyles = makeStyles({
    groupListbox: {
        maxHeight: "280px",
        overflowY: "auto",
        overscrollBehavior: "contain"
    }
});

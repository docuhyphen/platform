import {makeStyles} from "@fluentui/react-components";

export const useSessionDocumentZipDownloadDialogStyles = makeStyles({

    downloadContainer: {
        margin: "16px 0",
        display: "flex",
        flexDirection: "column",
        gap: "8px"
    },
    downloadNameField: {}
});
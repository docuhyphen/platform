import {makeStyles, tokens} from "@fluentui/react-components";

export const useAccessPermissionsPanelStyles = makeStyles({
    switchGroup: {
        display: "flex",
        flexDirection: "column",
        gap: tokens.spacingHorizontalM,
    },
});
